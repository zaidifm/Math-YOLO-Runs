# Blackboard provenance addendum — late 2026-08-11

This file prevents the late blackboard synthesis from being back-attributed to the historical BLIND sessions.

## Current-run discoveries / syntheses

The following were first made explicit in the 2026-08-11 ChatGPT audit after the historical archive had been assembled:

1. the exact source identity `xK-vL=1` and the matrix `[[x,v],[L,K]] in SL_2`;
2. the interpretation of the BLIND 2 boundary ring as the affine torus quotient of that `SL_2` model;
3. the discriminant-one quadric `q^2-4cW=1` and full `sl_2` triple extending the earlier LND;
4. the exact ideal pullback `(W,b)k[x,y,z]=(x)` strengthening the intersection proof;
5. the second-Veronese / `A_1` associated-graded explanation of the parity law;
6. the complete fixed-chart canonical-potential classification modulo translations by the boundary ideal `I_D=(W,b)`;
7. the all-hidden-degree minimal-direction spectrum and degree-efficient representatives;
8. the bridge `G=n_-(p)J_2^{-1}w` showing that the BLIND 4 root frame `J_1^{-1}` and the boundary frame belong to the same elementary `SL_2` family;
9. the complete nonzero-scalar quintic surjectivity classification with exceptional equation `27 lambda^2+99 lambda+5=0`;
10. the all-degree minimal-direction surjectivity theorem for every `m>=6`;
11. the Newton/Vandermonde second proof of the high-degree surjectivity rigidity;
12. the complete minimal-scalar phase diagram: `m=3,4` never surjective, `m=5` two exceptional nonzero scalars, `m>=6` always surjective.

These are **not** claims that the underlying mathematics is externally novel. They are provenance claims about what was and was not already explicit in the retained project corpus.

## Historical overlap that must be credited

BLIND 1 already contained:

- a general potential/deformation framework;
- examples of arbitrary generic fiber degree;
- a monomial surjective noninjective family for every generic degree `m>=6`;
- degree profile `(5m,5m-1,4)` for that family;
- a proof that its particular `m=5` monomial member is nonsurjective;
- the explicit statement that this did not rule out another generic-degree-five surjective deformation.

Therefore the late blackboard theorem must not be described as the first project discovery of arbitrary-degree surjective Keller maps.

The current-run sharpening relative to the retained archive is instead:

- the boundary-minimal mechanism;
- substantially lower degree profiles `(D_m,D_m-1,4)` with `D_m=3m+2` for odd `m` and `3m+4` for even `m`;
- fixed-chart minimality for those directions;
- and the affirmative resolution of the retained degree-five surjectivity question.

## Epistemic labels

Exact symbolic identities, eliminations, low-degree factorization classifications, and bounded matrix comparisons are COMPUTED.

General quotient/intersection/classification and all-degree surjectivity arguments are DERIVED with internal cross-checks and, in the high-degree surjectivity case, two independent blackboard proofs.

No external priority claim should be made until specialist review and the outstanding literature-coverage caveats are addressed.